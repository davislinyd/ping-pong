# Development mode API server base URL.
# If Vite is running on port 5173 and Fastify is on port 8080,
# set this to http://localhost:8080 to allow direct API calls.
VITE_API_BASE=http://localhost:8080
